{
  "name": "md-reader-android",
  "version": "1.1.0",
  "private": true,
  "description": "MDReader Android app build scripts",
  "scripts": {
    "build": "node scripts/android-build.js",
    "gradle:assembleDebug": "./gradlew assembleDebug",
    "gradle:assembleRelease": "./gradlew assembleRelease"
  }
}
